# You can also use wget
curl -X {{=data.methodUpper}} {{=data.url}}{{=data.requiredQueryString}} \
  -H 'Authorization: Bearer ACCESS_TOKEN'{{?data.allHeaders.length}} \{{?}}
{{~data.allHeaders :p:index}}  -H '{{=p.name}}: {{=p.exampleValues.object}}'{{?index < data.allHeaders.length-1}} \{{?}}
{{~}}
