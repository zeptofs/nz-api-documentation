{{?data.allHeaders.length}}var headers = {
{{~data.allHeaders :p:index}}  '{{=p.name}}': {{=p.exampleValues.json}}{{?index < data.allHeaders.length-1}},{{?}}
{{~}}};
{{?}}{{? data.bodyParameter.present}}

var body = '{{=data.bodyParameter.exampleValues.json}}';{{?}}

$.ajax({
  url: '{{=data.url}}',
  method: '{{=data.method.verb}}'{{? data.bodyParameter.present}},
  data: body,{{?}}{{?data.allHeaders.length}}
  headers: headers,{{?}}
  success: function(data) {
    console.log(JSON.stringify(data));
  }
})
